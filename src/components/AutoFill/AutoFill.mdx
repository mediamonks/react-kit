import { Canvas, Meta } from '@storybook/blocks';
import * as stories from './AutoFill.stories';

<Meta title="Components / AutoFill" />

# AutoFill

Repeats children to fill the parent element in given axis.

## Reference

```ts
interface AutoFillProps {
  children: ReadonlyArray<ReactNode>;
  axis?: 'x' | 'y'; // Default: 'x'
}

function AutoFill(props: AutoFillProps): ReactElement;
```

## AutoFill for X axis

Fills components horizontally.

```tsx
function DemoComponent() {
  return (
    <div style={{ width: 1000, display: 'grid', outline: '1px solid black' }}>
      <AutoFill>
        <div style={{ padding: 15 }}>
          <div style={{ width: 50, outline: '1px solid red' }}>Child</div>
        </div>
      </AutoFill>
    </div>
  );
}
```

### Demo

<Canvas of={stories.Horizontal} />

## AutoFill for Y axis

Fills components vertically.

```tsx
function DemoComponent() {
  return (
    <div style={{ height: 1000, outline: '1px solid black' }}>
      <AutoFill axis="y">
        <div style={{ padding: 15 }}>
          <div style={{ width: 50, outline: '1px solid red' }}>Child</div>
        </div>
      </AutoFill>
    </div>
  );
}
```

### Demo

<Canvas of={stories.Vertical} />
