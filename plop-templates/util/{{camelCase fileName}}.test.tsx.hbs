import { describe, it } from 'vitest';
import { {{camelCase fileName}} } from './{{camelCase fileName}}.js';

describe('{{camelCase fileName}}', () => {
  it('should not crash', async () => {
    const {{camelCase fileName}}Value = {{camelCase fileName}}();

    expect({{camelCase fileName}}Value).toBe('null');
  });
});
